/**
 * Ledger — 화면과 서버가 함께 지켜야 하는 수 (§11.4)
 *
 * 서버 액션 파일('use server')에서는 함수 말고 아무것도 내보낼 수 없다.
 * 그래서 두 쪽이 같이 알아야 하는 상한은 여기 둔다. 두 벌로 적어 두면
 * 한쪽만 고쳐지고, 화면은 스무 줄을 보내는데 서버는 열 줄에서 자른다.
 */

/**
 * 한 번에 적을 수 있는 줄의 수.
 *
 * 사진 열 장이 현실적인 한 묶음이다. 스물이 넘으면 읽는 값도, 사람이
 * 확인해야 할 것도 한 화면을 넘어선다 — 확인하지 않고 저장하는 순간
 * 이 기능은 장부를 더럽히는 기능이 된다.
 */
export const MAX_BATCH = 20;
